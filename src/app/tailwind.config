```ts
// tailwind.config.ts
import type { Config } from "tailwindcss";

export default {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  theme: {
    extend: {
      fontFamily: {
        sans: ["ui-sans-serif", "system-ui", "Inter", "SF Pro Display", "Segoe UI", "Roboto", "Arial", "sans-serif"],
      },
      boxShadow: {
        glass: "0 20px 60px rgba(0,0,0,0.55)",
      },
    },
  },
  plugins: [],
} satisfies Config;
```